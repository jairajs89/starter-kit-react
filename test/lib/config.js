describe('Config', ()=> {
    it('has config and injects overrides from query string', ()=> {
        // Enable when we actual configs
        // const url1 = 'http://sup.com/';
        // const url2 = 'http://localhost:5000/';
        // const host = 'override.com';
        // require('window').location.href = `http://${host}/?url2=${url2}`;
        // const Config = require('../../src/lib/config');
        // expect(Config.url1).toEqual(url1);
        // expect(Config.url2).toEqual(url2.replace('localhost', host));
    });
});
